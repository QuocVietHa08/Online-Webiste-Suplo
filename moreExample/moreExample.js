$(document).ready(function () {
  $('.your-class').slick({
    // autoplay: true,
    // autoplaySpeed: 1000,
    infinite: true,
    slidesToShow: 3,
    slidesToScroll: 1,
    prevArrow: '.arrow-left',
    nextArrow: '.arrow-right',
  });
});
